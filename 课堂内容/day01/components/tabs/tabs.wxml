<!--components/tabs/tabs.wxml-->

<view class="box">
    <view wx:for="{{list}}" wx:key="index" class="{{current == index ? 'active' : ''}}" bindtap='handClick' data-index="{{index}}">
        {{item}}
    </view>
</view>